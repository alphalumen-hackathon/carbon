import { TouchableOpacity } from "react-native";

import { styles } from "./TopModalButton.style";

const TopModalButton = () => {
  return <TouchableOpacity style={styles.topButton} />;
};

export default TopModalButton;
